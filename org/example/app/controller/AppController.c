#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "../service/Func.h"

int run() {
    srand(time(NULL));
    if ()
    int row = random_num(1,5);
    int col = random_num(1,5);

    printf("Running...\n");
}
